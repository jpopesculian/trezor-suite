{
    "extends": "../../tsconfig.lib.json",
    "compilerOptions": {
        "outDir": "lib",
        // REF-TODO: target? es2020 has bigint. previously we had es2019
        "target": "es2020",
        "importHelpers": true,
        // REF-TODO: we need this to avoid runtime changes. could be refactored.
        "strictPropertyInitialization": false
    },
    "include": ["./src"]
}
